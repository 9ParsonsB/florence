sources = [
  files(
    'controller.c',
    'florence.c',

    )]

cflags = [
  '-DICONDIR="@0@"'.format(icondir),
  '-DFLORENCELOCALEDIR="@0@"'.format(florencelocaledir),
  '-DDATADIR="@0@"'.format(datadir),
  ]